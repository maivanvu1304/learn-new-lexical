{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/api/index.ts",
    "build": "echo \"No backend build step configured yet\"",
    "test": "tsx --test tests/**/*.spec.ts",
    "lint": "echo \"Lint configuration pending\""
  },
  "dependencies": {
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
